/**
 * Special package containing tests that prove known bugs exist.  All tests in this package are
 * currently failing and thus are ignored to avoid affecting build stability.  When these tests
 * are made to pass (i.e., the corresponding bugs are fixed) then they should be moved out of
 * this package into their rightful places.
 */
package org.neo4j.ogm.defects;